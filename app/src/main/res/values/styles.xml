<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- 滑动结束activity -->
    <style name="Theme.Slidr.Slidable" parent="AppTheme">

        <!-- These values must be set for this mechanism to work -->
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>


    <!--背景无模糊-->
    <style name="dialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item><!--边框-->
        <item name="android:windowIsFloating">true</item><!--是否浮现在activity之上-->
        <item name="android:windowIsTranslucent">false</item><!--半透明-->
        <item name="android:windowNoTitle">true</item><!--无标题-->
        <item name="android:windowBackground">@color/toming</item><!--背景透明-->
        <item name="android:backgroundDimEnabled">false</item><!--模糊-->
    </style>


    <!-- banner轮播圆点 -->
    <style name="MaterialIndicator.Demo">
        <item name="mi_indicatorColor">@color/bg_main_bottom</item>
        <item name="mi_indicatorPadding">24dp</item>
        <item name="mi_indicatorRadius">4dp</item>
    </style>

    <style name="MaterialIndicator">
        <item name="mi_indicatorRadius">4dp</item>
        <item name="mi_indicatorColor">#ff009688</item>
    </style>
        <style name="MyCheckBox" parent="@android:style/Widget.CompoundButton.CheckBox">

            <item name="android:button">@drawable/checkbox_selector</item>

            <item name="android:paddingLeft">15.0dip</item>


        </style>
    <style name="MyDialog" parent="android:style/Theme.Dialog">
        <item name="android:windowNoTitle">true</item>
        <item name="android:background">#00000000</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>
    <style name="InputTextView">
        <item name="android:textColor">#000000</item>
        <item name="android:gravity">center</item>
        <item name="android:inputType">textPassword</item>
        <item name="android:textSize">40sp</item>
    </style>
    <style name="TextView">
        <item name="android:textColor">@color/white</item>
        <item name="android:gravity">center</item>
    </style>
    <style name="SelectTextView">
        <item name="android:textColor">@color/tab_line_color</item>
        <item name="android:gravity">center</item>
    </style>
</resources>
